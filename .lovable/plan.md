

# DemoBanner Copy & CTA Update

## Changes to `src/components/DemoBanner.tsx`

1. **Description text** (line 30-32): Replace current body copy with:
   *"Try the Intelligent Story Builder — an agentic AI tool that reduced story creation time by 80% across 20+ product teams. Select a scenario, watch the AI work, and see real output."*

2. **CTA button text** (line 37): Change from `TRY THE LIVE DEMO →` to `TRY THE LIVE DEMO` (remove the arrow, or keep it -- current code already says "TRY THE LIVE DEMO →").

No layout, icon, or animation changes. Title "See AI in Action" stays as-is.

## Technical Detail

Single file edit to `src/components/DemoBanner.tsx`:
- Update the two `<p>` tags (subheadline + body) to the new combined copy
- Confirm CTA reads "TRY THE LIVE DEMO"

